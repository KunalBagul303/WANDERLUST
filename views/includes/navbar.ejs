
<style>
  .search-btn {
    background-color: #fe424d;
    color: #fff;
    border-radius: 25px;
    padding: 0 1rem 0 1rem;
  }

  .form-inline {
    display: flex;
    gap: 0.7rem;
  }
    .search-btn:hover {
    background-color: #fe424d;
    color: #fff;
  }

  .search-btn i {
    display: inline;
    margin-right: 0.5rem;
  }

  .search-inp {
    border-radius: 25px;
    padding: 0.5rem 3rem 0.5rem 3rem;
    font-size: 1rem;
  }
 
/* Normal link color */
.nav-link {
  text-decoration: none;
  font-weight: 500;
  color: black;
}

/* Gradient color for SignUp */
.signup {
  background: linear-gradient(90deg, #00c6ff, #0072ff); /* green-blue mix */
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}

/* Gradient color for LogIn */
.login {
  background: linear-gradient(90deg, #0072ff, #00c6ff); /* blue-green mix */
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}

/* Hover effect */
.signup:hover,
.login:hover {
  text-decoration: underline;
}

.logout{
  color: #fe424d !important;
}


</style>

<nav class="navbar navbar-expand-md bg-body-light border-bottom sticky-top">
  <div class="container-fluid">
    <a class="navbar-brand" href="/listings"><i class="fa-regular fa-compass"></i></a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-link" href="/listings">Explore</a>
      </div>
      <div class="navbar-nav ms-auto">
         <form class="form-inline">
            <input class="form-control mr-sm-2 search-inp" type="search" placeholder="Search destinations">
            <button class="btn search-btn my-2 my-sm-0" type="submit"><i class="fa-solid fa-magnifying-glass"></i>Search</button>
          </form>
      </div>
      <div class="navbar-nav ms-auto">
          <a class="nav-link" href="/listings/new">Airbnb your home</a>
        <% if (!currUser) { %> 
        <a class="nav-link signup"  href="/signup"><b>Sign up</b></a>
        <a class="nav-link login" href="/login"><b>Log in</b></a>
         <% } %>
         <% if (currUser) { %> 
        <a class="nav-link logout" href="/logout"><b>Log out</b></a>
        <% } %>
      </div>
    </div>
  </div>
</nav>